import { Swatch } from "./swatch";

const C = new Swatch();

export class Border {
  card = C.gray20;
  buttonPrimary = C.blue60;
  focusPrimary = C.blue60;
  error = C.red60;
  light = C.blue10;
  medium = C.gray40;
  darkest = C.blue100;
}
