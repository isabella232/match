import { coreThemeTokens } from "./designs/CoreTheme.sketch";
const { colors: C } = coreThemeTokens;

export class TextColors {
  primary = C.gray90;
  secondary = C.gray70;
  tertiary = C.gray60;
  inversePrimary = C.white;
}
