import { Colors } from "../../types";

import { color } from "./color";

export const textColor: Colors = {
  primary: color.gray90,
  secondary: color.gray70,
  tertiary: color.gray60,
  inversePrimary: color.white,
};
