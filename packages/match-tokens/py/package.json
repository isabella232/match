{
  "name": "match_tokens_py",
  "version": "0.0.0",
  "description": "Match token library for Python",
  "homepage": "https://match.twilio.design",
  "author": "Twilio Inc.",
  "license": "UNLICENSED",
  "private": true,
  "main": "src/index.js",
  "repository": {
    "type": "git",
    "url": "ssh://git@github.com/twilio-labs/match",
    "directory": "packages/match-tokens/pypi"
  },
  "scripts": {
    "build": "yarn clean && yarn compile",
    "clean": "rm -rf match_tokens_py build dist match_tokens_py.egg-info",
    "compile": "node src/index.js && python3 setup.py sdist bdist_wheel",
    "publish": "twine upload dist/*",
    "publish:test": "twine upload --repository testpypi dist/*"
  },
  "devDependencies": {
    "@twilio-labs/match-tokens": "workspace:packages/match-tokens/bundle",
    "ejs": "^3.1.5"
  }
}
