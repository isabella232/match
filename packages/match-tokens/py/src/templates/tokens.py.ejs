swatch = {
<% Object.entries(swatch).forEach(([name, token]) => { -%>
    '<%=name%>': '<%=token.color%>',
<% }); -%>
}

background = {
<% Object.entries(background).forEach(([name, token]) => { -%>
    '<%=name%>': '<%=token.color%>',
<% }); -%>
}

border = {
<% Object.entries(border).forEach(([name, token]) => { -%>
    '<%=name%>': '<%=token.color%>',
<% }); -%>
}

text = {
<% Object.entries(text).forEach(([name, token]) => { -%>
    '<%=name%>': '<%=token.color%>',
<% }); -%>
}

gradient = {
<% Object.entries(gradient).forEach(([name, token]) => { -%>
    '<%=name%>': '<%=token.linearGradient%>',
<% }); -%>
}

icon_size = {
<% Object.entries(iconSize).forEach(([name, token]) => { -%>
    '<%=name%>': '<%=token.rem%>',
<% }); -%>
}
