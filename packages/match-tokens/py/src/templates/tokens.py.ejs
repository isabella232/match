colors = {
<% Object.entries(colors).forEach(([name, token]) => { -%>
    '<%=name%>': '<%=token%>',
<% }); -%>
}

background_colors = {
<% Object.entries(backgroundColors).forEach(([name, token]) => { -%>
    '<%=name%>': '<%=token%>',
<% }); -%>
}

border_colors = {
<% Object.entries(borderColors).forEach(([name, token]) => { -%>
    '<%=name%>': '<%=token%>',
<% }); -%>
}

text_colors = {
<% Object.entries(textColors).forEach(([name, token]) => { -%>
    '<%=name%>': '<%=token%>',
<% }); -%>
}

gradients = {
<% Object.entries(gradients).forEach(([name, token]) => { -%>
    '<%=name%>': '<%=token%>',
<% }); -%>
}

icon_sizes = {
<% Object.entries(iconSizes).forEach(([name, token]) => { -%>
    '<%=name%>': '<%=token%>',
<% }); -%>
}
