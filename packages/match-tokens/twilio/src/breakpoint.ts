import { Breakpoint as Default } from "@twilio-labs/match-tokens-core";

export const Breakpoint = {
  small: Default.small,
  medium: Default.medium,
  large: Default.large,
  xLarge: Default.xLarge,
};

console.log(Breakpoint);
