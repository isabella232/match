import { Breakpoint } from "./breakpoint";
import { Droppings as Default } from "@twilio-labs/match-tokens-core";

const Droppings = {
  hamburger: Default.hamburger,
};

export const twilioDesignTokens = {
  Breakpoint,
  Droppings,
};
