---
id: develop
title: Develop
sidebar_label: Develop
slug: /getting-started/develop/
---

👋 Hi Twilio developer. Follow the steps below to begin building with Match

## 1. Install external dependencies

| Package           | Version |
| ----------------- | ------- |
| prop-types        | 15.x    |
| react             | 17.x    |
| react-dom         | 17.x    |
| react-is          | 17.x    |
| react-uid         | 2.x     |
| styled-components | 5.x     |

### Install dependencies using yarn

```shell npm2yarn
npm install --save prop-types react react-dom react-is styled-components
```

## 2. Configure GitHub Package Registry

The Match packages are hosted on the GitHub Package Registry and require some additional configuration to install.

For Yarn 2 and up, add the following to the .yarnrc.yml file in the root of your project:

```yaml
npmScopes:
  twilio-labs:
    npmRegistryServer: "https://npm.pkg.github.com"
```

For Yarn 1, add the following to the .yarnrc file in the root of your project.

```
"@twilio-labs:registry" "https://npm.pkg.github.com"
```

For NPM, add the following to the .npmrc file in the root of your project.

```
@twilio-labs:registry=https://npm.pkg.github.com
```

## 3. Install Match packages

Install the following packages so you can consume Match’s tokens and themes to build custom page sections.

| Package                          | Version       |
| -------------------------------- | ------------- |
| @twilio-labs/match               | 1.0.0-alpha.1 |
| @twilio-labs/match-icons-twilio  | 1.0.0-alpha.1 |
| @twilio-labs/match-icons-product | 1.0.0-alpha.1 |

```bash npm2yarn
npm install --save @twilio-labs/match @twilio-labs/match-icons-twilio @twilio-labs/match-icons-product
```
